using Game.Progression;
using UnityEngine;

public class Tester : MonoBehaviour
{
    

    // Update is called once per frame
    void Update()
    {
        /*if(Input.GetKeyDown(KeyCode.Alpha1))
        {
            //Debug.Log("Increasing health");
            PlayerProgression.Instance.UpdateStat(StatType.MaxHealth, 10);
        }
        if (Input.GetKeyDown(KeyCode.Alpha2))
        {
            //Debug.Log("Increasing armor");
            PlayerProgression.Instance.UpdateStat(StatType.Armor, 1);
        }
        if (Input.GetKeyDown(KeyCode.Alpha3))
        {
            //Debug.Log("Increasing armor");
            PlayerProgression.Instance.UpdateStat(StatType.MeleeDamage, 1);
        }
        if (Input.GetKeyDown(KeyCode.Alpha4))
        {           
            PlayerProgression.Instance.UpdateStat(StatType.AttackSpeed, 1);
        }
        if (Input.GetKeyDown(KeyCode.Alpha5))
        {           
            PlayerProgression.Instance.UpdateStat(StatType.MoveSpeed, 1);
        }
        if (Input.GetKeyDown(KeyCode.Alpha6))
        {
            PlayerProgression.Instance.UpdateStat(StatType.DashCooldown, 1);
        }
        if (Input.GetKeyDown(KeyCode.Alpha7))
        {
            PlayerProgression.Instance.UpdateStat(StatType.MaxGrace, 1);
        }*/
    }
}
